@model TrainingSystem.API.Models.RoomEquipment

@{
    ViewData["Title"] = "Delete Room Equipment";
}

<div class="container-fluid py-5 px-3 px-md-5">
    <div class="row justify-content-center">
        <div class="col-12 col-xl-8">
            <div class="card border-0 shadow-sm" style="border-radius: 18px;">
                <div class="card-body p-4 p-lg-5">

                    <div class="mb-4">
                        <h3 class="fw-bold mb-2 text-danger">Delete Room Equipment</h3>
                        <p class="text-muted mb-0">Are you sure you want to delete this room equipment record?</p>
                    </div>

                    <div class="mb-4 p-3 rounded-3" style="background-color: #f8f9fc;">
                        <div class="mb-2">
                            <span class="fw-semibold" style="color: #0a1f5c;">Room:</span>
                            <span class="text-muted">@Model.Room?.RoomName</span>
                        </div>

                        <div class="mb-2">
                            <span class="fw-semibold" style="color: #0a1f5c;">Equipment:</span>
                            <span class="text-muted">@Model.Equipment?.EquipmentName</span>
                        </div>

                        <div>
                            <span class="fw-semibold" style="color: #0a1f5c;">Quantity:</span>
                            <span class="text-muted">@Model.Quantity</span>
                        </div>
                    </div>

                    <form asp-action="Delete" method="post">
                        @Html.AntiForgeryToken()
                        <input asp-for="RoomEquipmentId" type="hidden" />

                        <div class="d-flex justify-content-between align-items-center">
                            <a asp-action="Index"
                               class="btn btn-light fw-semibold"
                               style="border-radius: 10px;">
                                Cancel
                            </a>

                            <button type="submit"
                                    class="btn text-white fw-semibold px-4"
                                    style="background-color: #dc3545; height: 48px; border-radius: 10px; border: none;">
                                Delete
                            </button>
                        </div>
                    </form>

                </div>
            </div>
        </div>
    </div>
</div>