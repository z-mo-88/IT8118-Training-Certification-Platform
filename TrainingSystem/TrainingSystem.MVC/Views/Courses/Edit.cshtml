@model TrainingSystem.API.Models.Course

@{
    ViewData["Title"] = "Edit Course";
}

<div class="container-fluid py-5 px-3 px-md-5">
    <div class="row justify-content-center">
        <div class="col-12 col-xl-9">
            <div class="card border-0 shadow-sm" style="border-radius: 18px;">

                <div class="card-body p-4 p-lg-5">
                    <div class="mb-4">
                        <h3 class="fw-bold mb-2" style="color: #0a1f5c;">Edit Course</h3>
                        <p class="text-muted mb-0">Update the course details below.</p>
                    </div>

                    @if (!ViewData.ModelState.IsValid)
                    {
                        <div asp-validation-summary="All" class="alert alert-danger rounded-3 mb-4"></div>
                    }

                    <form asp-action="Edit" method="post">
                        <input type="hidden" asp-for="CourseId" />

                        <div class="mb-3">
                            <label class="form-label fw-semibold" style="color: #0a1f5c;">Title</label>
                            <input asp-for="Title"
                                   class="form-control"
                                   placeholder="Enter course title"
                                   style="height: 50px; border-radius: 10px;" />
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-semibold" style="color: #0a1f5c;">Description</label>
                            <textarea asp-for="Description"
                                      class="form-control"
                                      placeholder="Enter course description"
                                      style="border-radius: 10px; min-height: 100px;"></textarea>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-semibold" style="color: #0a1f5c;">Category</label>
                            <select asp-for="CategoryId"
                                    asp-items="ViewBag.Categories"
                                    class="form-select"
                                    style="height: 50px; border-radius: 10px;">
                            </select>
                        </div>

                        <div class="row">
                            <div class="col-md-4 mb-3">
                                <label class="form-label fw-semibold" style="color: #0a1f5c;">Duration</label>
                                <input asp-for="DurationHours"
                                       class="form-control"
                                       style="height: 50px; border-radius: 10px;" />
                            </div>

                            <div class="col-md-4 mb-3">
                                <label class="form-label fw-semibold" style="color: #0a1f5c;">Capacity</label>
                                <input asp-for="DefaultCapacity"
                                       class="form-control"
                                       style="height: 50px; border-radius: 10px;" />
                            </div>

                            <div class="col-md-4 mb-3">
                                <label class="form-label fw-semibold" style="color: #0a1f5c;">Fee</label>
                                <input asp-for="EnrollmentFee"
                                       class="form-control"
                                       style="height: 50px; border-radius: 10px;" />
                            </div>
                        </div>

                        <div class="mb-4">
                            <label class="form-label fw-semibold" style="color: #0a1f5c;">Prerequisite</label>
                            <select asp-for="PrerequisiteCourseId"
                                    asp-items="ViewBag.Courses"
                                    class="form-select"
                                    style="height: 50px; border-radius: 10px;">
                                <option value="">None</option>
                            </select>
                        </div>

                        <div class="d-flex justify-content-between align-items-center">
                            <a asp-action="Index"
                               class="btn btn-light fw-semibold"
                               style="border-radius: 10px;">
                                Back
                            </a>

                            <button type="submit"
                                    class="btn fw-semibold px-4 text-white"
                                    style="background-color: #0a1f5c; height: 48px; border-radius: 10px; border: none;">
                                Save Changes
                            </button>
                        </div>

                    </form>

                </div>
            </div>
        </div>
    </div>
</div>